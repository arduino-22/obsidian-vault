---
icon: 🗒
target:
- ""
createdAt: "{{DATE}}"
imageNameKey: nota_<% tp.file.title.split(" ").join("_").toLowerCase() %>
tags: 
NoteRel:
---
## Чек-лист определения локальных машины
- [ ] Имя пользователя и группа
- [ ] Имена пользователей, группы машины и имя хоста
- [ ] Средства окружения
- [ ] Конфигурация bashrc
- [ ] История выполнения Bash
- [ ] Информация о системе, операционная система и версия ядра
- [ ] Версия sudo
- [ ] Определение процессов
- [ ] Регулярные задачи
- [ ] Установленные пакеты
- [ ] Установленные и удаленные устройства
- [ ] Интерфейсы и маршруты
- [ ] Модули ядра

## PrivEsc
- [ ] Директории и файлы с правом записи
- [ ] Экземпляры программ с suid-правилами
- [ ] Проверка паролей в процессах
- [ ] Проверка паролей в tcpdump
- [ ] Поиск чувствительных файлов
- [ ] Уязвимости версий ядра

## Инструменты
- [ ] Linpeas
- [ ] unix-privesc-check

# Определение для пользователя (USER)

## Имя пользователя и группа
```
whoami
id

```

## Средства окружения
```
env
```


## Конфигурация bashrc
```

```

## История выполнения Bash
```
history
```

## SSH-ключи ?
```
~/.ssh/*

```

# Информация о системе

## Пользователи, группы и имя хоста
```
cat /etc/passwd

cat /etc/groups

hostname
```

## Операционная система и версия ядра
```
cat /etc/os-release 

cat /etc/issue

uname -a

```
## Версия sudo
```
sudo -l 
```
## Определение процессов
```
ps aux
```

## Регулярные задачи
```
ls -lah /etc/cron*

crontab -l

sudo crontabk -l

grep "CRON" /var/log/syslog


cat /etc/crontab
```

## Значимые установленные пакеты 
```
dpkg -l 
```

## Интерфейсы и маршруты
```
ifconfig

ip a

route 

routel
```

## Модули ядра
```
lsmod
```

Определение информации о модуле ядра
```
/sbin/modinfo libata
```
## Устройства, установленные и удаленные
```
cat /etc/fstab

mount

lsblk
```

# Повышение привилегий

## Директории и файлы с правом записи
```
find / -writable -type d 2>/dev/null
```

## Экземпляры программ с suid-правилами
```
find / -perm -u=s -type f 2>/dev/null
```

## Поиск экземпляров программ с возможностью эксплуатации возможностями
```
/usr/sbin/getcap -r / 2>/dev/null
```

## Проверка паролей в процессах
```
watch -n 1 "ps -aux | grep pass"
```

## Проверка паролей в tcpdump
```
sudo tcpdump -i -lo -A | grep "pass"
```

## Поиск чувствительных файлов 
```
find / -name *txt, *config, *conf*, *docx* 
```
